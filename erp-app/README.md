# ERP 시스템 - 재고관리 & 인사관리

Electron + React + TypeScript 기반의 데스크탑 ERP 애플리케이션입니다.

## 주요 기능

### ✅ 구현 완료
- **로그인 시스템**: 관리자/일반 직원 권한 구분
- **재고관리**:
  - 제품 등록
  - 제품 목록 조회
  - 제품 수정/삭제
  - 엑셀 데이터 일괄 임포트
  - 검색 및 필터링
  - 실시간 재고 통계
- **인사관리**:
  - **직원 목록**: 직원 정보 조회, 검색, 통계 (5명 샘플 데이터)
  - **근태 관리**: 
    - 근태 현황 대시보드 (근무시간, 초과시간)
    - 연차 현황 (서비스 연차 70.9%, 특이사항)
    - 2024년 근무 통계 (정시 퇴근율 70%)
    - 이번주 근무 기록 캘린더
    - 근무 신청 현황 (대기중/승인/반려)
  - **휴가 관리**: 
    - 휴가 신청 목록
    - 통계 카드 (전체/대기/승인/반려)
    - 휴가 필터링 (전체/대기중/승인/반려)
    - 승인/반려 기능 UI

### 🔄 데이터베이스
- SQLite (로컬 파일 기반)
- 추후 MySQL/PostgreSQL로 쉽게 전환 가능한 구조

## 설치 및 실행

### 1. 개발 모드 실행

```bash
cd erp-app
npm run electron:dev
```

### 2. 실행 파일 빌드

```bash
npm run electron:build
```

빌드된 실행 파일은 `release` 폴더에 생성됩니다.

## 기본 로그인 정보

- **아이디**: `admin`
- **비밀번호**: `admin123`
- **권한**: 관리자

## 엑셀 데이터 임포트

1. 프로젝트 루트에 `products_import.csv` 파일이 준비되어 있습니다.
2. 앱 실행 후 **재고관리 > 제품 목록** 메뉴로 이동
3. **엑셀 임포트** 버튼 클릭
4. 220개의 제품이 자동으로 임포트됩니다!

## 프로젝트 구조

```
erp-app/
├── electron/          # Electron 메인 프로세스
│   ├── main.js       # 메인 프로세스 (데이터베이스, IPC)
│   └── preload.js    # Preload 스크립트
├── src/
│   ├── components/   # React 컴포넌트
│   │   ├── Layout.tsx
│   │   └── Sidebar.tsx
│   ├── context/      # Context API
│   │   └── AuthContext.tsx
│   ├── pages/        # 페이지 컴포넌트
│   │   ├── Login.tsx
│   │   ├── inventory/
│   │   │   ├── ProductRegister.tsx
│   │   │   └── ProductList.tsx
│   │   └── hr/
│   │       ├── Employees.tsx
│   │       ├── Attendance.tsx
│   │       └── Leaves.tsx
│   ├── types/        # TypeScript 타입 정의
│   └── App.tsx
└── package.json
```

## 데이터베이스 스키마

### Users (사용자)
- id, username, password, name, role, created_at

### Products (제품)
- id, barcode, product_name, quantity, consumer_price, purchase_price, month, created_at, updated_at

### Employees (직원)
- id, user_id, employee_code, department, position, hire_date, phone, email, created_at

### Attendance (근태)
- id, employee_id, date, check_in, check_out, status, created_at

### Leaves (휴가)
- id, employee_id, leave_type, start_date, end_date, reason, status, created_at

## 기술 스택

- **Frontend**: React 19 + TypeScript + Tailwind CSS
- **Desktop**: Electron
- **Database**: SQLite (better-sqlite3)
- **Build**: Vite + electron-builder
- **Icons**: lucide-react
- **Router**: React Router v6

## 다음 단계

1. ✅ 재고관리 완성
2. 🚧 인사관리 기능 상세 구현
3. 🚧 제품 수정 기능 UI 추가
4. 🚧 통계 및 대시보드
5. 🚧 웹 버전으로 확장 (백엔드 API 추가)

## 라이선스

MIT
