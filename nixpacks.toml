# Railway Nixpacks 설정
[phases.setup]
nixPkgs = ["nodejs_22", "python3"]

[phases.install]
cmds = ["cd erp-app && npm install"]

[phases.build]
cmds = ["cd erp-app && npm run build"]

[start]
cmd = "cd erp-app && npm run server"
